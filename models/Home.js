export default class Home {
  constructor(id, imageURL, title, price, quantity) {
    this.id = id;
    this.imageURL = imageURL;
    this.title = title;
    this.price = price;
    this.quantity = quantity;
  }
};